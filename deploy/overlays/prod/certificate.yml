apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: laurel-tls
  namespace: prod
spec:
  secretName: laurel-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - laurel.andreslobaton.top
